import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Our Services | BuffIndia",
  description:
    "Cigarette waste kiosks, collection, recycling, KraftReborn products, and monthly ESG impact reporting.",
}

export default function ServicesLayout({ children }: { children: React.ReactNode }) {
  return children
}
